server.port=8080
server.servlet.context-path=/backend
spring.datasource.username=root
spring.datasource.password=devinhouse
spring.jpa.hibernate.ddl-auto=update
# spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.url=jdbc:h2:mem:db
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.H2Dialect

debug = true
logging.level.root = INFO
logging.file.name = spring.log

keycloak.realm =  grupo4
keycloak.auth-server-url =  https://training.dev.delivery/auth/
keycloak.ssl-required =  external
keycloak.resource =  projeto6
keycloak.public-client =  true
keycloak.confidential-port =  0
keycloak.bearer-only = true
keycloak.cors = true

keycloak.securityConstraints[0].authRoles[0]=user
keycloak.securityConstraints[0].securityCollections[0].name=secured content
keycloak.securityConstraints[0].securityCollections[0].patterns[0]=/*

# server.port=8080
# server.servlet.context-path=/backend
# spring.datasource.url=jdbc:mysql://localhost:3306/listagem_processos?createDatabaseIfNotExist=true&autoReconnect=true&useSSL=false&allowPublicKeyRetrieval=true
# spring.datasource.username=root
# spring.datasource.password=devinhouse
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
