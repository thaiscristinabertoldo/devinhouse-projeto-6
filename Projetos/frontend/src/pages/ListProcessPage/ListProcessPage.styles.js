import { createStyles } from '@material-ui/core';

export const styles = createStyles({
  search: {
    marginBottom: 10,
  },
  addButton: {
    position: 'sticky',
    bottom: 15,
    marginTop: -55,
    width: '90%',
  },
});
